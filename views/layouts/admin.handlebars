<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>大流量王 admin</title>
  {{{css "admin"}}}
</head>
<body>
  {{{js "admin"}}}
  {{> header}}
  <div class="container-fluid">
    <div class="row">
      <div id="siderbar"  class="col-sm-3 col-md-2 sidebar">
        {{> sidebar}}
      </div>

      <div class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
        {{flash}}
        {{#if info}}
          {{ successTips info }}
        {{else}}
          {{ errTips err }}
        {{/if}}
        {{{body}}}
      </div>
    </div>
  </div>

  <hr>

  {{{_sections.javascript}}}
</body>
</html>